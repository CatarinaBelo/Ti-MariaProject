
spring:
  application:
    name: Ti-MariaProject
  datasource:
    url: jdbc:postgresql://localhost:5432/TiMaria
    username: postgres
    password: newpassword
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
